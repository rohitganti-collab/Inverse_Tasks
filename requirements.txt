# Match the MCP range in Taiga's canonical taiga-core 0.4.3 snapshot. The
# generated lock file pins the exact transitive build; widen this range only
# after the real-MCP selftest has passed.
mcp>=1.3.0,<=1.9.4
pydantic>=2.0,<3
