# Reasoning trap — <title>

<!-- The single professional error this task is built to catch. YOU write this. -->

## The tempting shortcut

<!-- One paragraph, concrete. Name the actual default: which resolution, which
     method, which convention, and what the solver does with the result.
     "Run once at the default resolution and report the value" — not "the model
     might be sloppy".

     It must be an error a competent colleague would really make. The defaults
     have to look like a reasonable starting point, not an obvious mistake. -->

## Why it fails silently

<!-- One paragraph. Why does the naive run COMPLETE CLEANLY and return a
     plausible number that is nonetheless wrong? This is the crux of a forward
     trap: if the failure were loud, the model would notice and fix it.

     Say what the wrong number looks like — ideally the right order of
     magnitude, the right sign, and physically unremarkable. -->

## Why a careful solver succeeds

<!-- One paragraph. The specific step the careful solver takes that the naive one
     skips — the second run at a finer setting, the transient rather than
     steady-state treatment, the convention checked rather than assumed. -->

<!-- REMEMBER: none of this may leak into problem.md, into `spec` or `help`
     output, or into any value the `run` mode returns. In particular the oracle
     must not report a residual or error estimate the model could threshold —
     recognising an inadequate setting is the skill being tested. -->
