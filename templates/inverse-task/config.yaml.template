# Task metadata. Keep values clean — no inline comments after a value.
#
# `direction` must match the folder: an inverse task has oracle/setup.py and no
# simulation/. The validator fails the problem if they disagree.

direction: inverse
domain: TODO            # your assigned domain
sub_domain: TODO        # e.g. enzyme_kinetics, circuit_analysis, phylogenetics
simulator: TODO         # the pinned tool your oracle uses, e.g. scipy, ngspice
title: TODO             # one short line, shown in listings
