"""Shortcut (trap) solver for <problem-id> — the wrong path, made concrete.

This file MUST FAIL 32/32. If it ever passes, the task is broken: the trap is
not actually a trap, and you have learned that here rather than from the gate.

It is not enough that it fails. It must fail ONTO THE NAMED NEAR-MISS from
grader/grading_guide.md — that is what makes the ≥60% failure-concentration
check meaningful. Run it and look at what it returns.

The shortcut has to be genuinely TEMPTING: the error a competent colleague
would actually make, not a strawman nobody would commit. It should also be
CHEAPER in budget than the intended path — that is what makes it attractive.

Same contract as main.py: `solve(oracle)` takes a fresh Oracle, returns the
answer in golden/expected.json's shape. NOT shipped to the gym — this stays on
the authoring side.

Common shortcuts worth implementing:
  * One mode only, no cross-check against a second observation
  * Two far-apart samples where the structure hides something in between
  * A default convention applied without checking (sign, denominator, units)
  * A memorised textbook constant instead of a measured one
  * Ignoring noise / not averaging when the reading is noisy
  * Stopping at the first plausible-looking value
"""
from __future__ import annotations


def solve(oracle):
    """The tempting-but-wrong path. Must land on the named near-miss.

    Args:
        oracle: a fresh Oracle instance.

    Returns:
        The near-miss answer — right shape, wrong value.
    """
    # The cheap, naive read. Replace with the trap from reasoning_trap.md.
    y = oracle.query("evaluate", x=10)

    naive_answer = ...  # TODO: the near-miss this produces

    return naive_answer
