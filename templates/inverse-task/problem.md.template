<!--
problem.md — THE ONLY TASK CONTENT THE MODEL SEES.

On Taiga this is either pasted into the Task Prompt form field (preferred) or
left here as problem.md and shipped. The form wins when both exist.

WRITE THIS LAST — Step 8 in INSTRUCTIONS.md. Not before the answer is locked,
the oracle is built, and the intended solver passes while the wrong-path solver
fails.

YOU write this, in your own words. Per the AI Use Policy, Claude must never
draft the solver-facing prompt.

State the setup and the budget. Nothing about method.

DO NOT DOCUMENT THE TOOL CALL SYNTAX. The container appends a generated calling
guide built from your ACTIONS, so anything you write here about
query_oracle(...) signatures is duplicated and can drift out of date. Name the
modes your prompt refers to — those names must match your ACTIONS names — and
leave the mechanics to the generated guide.

CHECKLIST before saving:
  [ ] The answer value appears nowhere in this file (the validator fails on it)
  [ ] The error you are testing for is not named, hinted, or gestured at — and
      it is not in an action description or help() output either
  [ ] No method names (the technique, algorithm, or transform to use)
  [ ] No canonical/memorised target the answer could be recalled from
  [ ] No strategy hints ("start by measuring...", "note that nearby inputs...")
  [ ] Clear of the validator's leakage vocabulary — INSTRUCTIONS.md lists the
      words it flags in solver-visible text
  [ ] Every constant the solver is ALLOWED to know is stated
  [ ] The budget is stated and matches Oracle.BUDGET
  [ ] Mode names match your ACTIONS names
  [ ] Answer shape matches golden/expected.json and Oracle.ANSWER_SCHEMA,
      element for element, in order

DELETE THIS COMMENT BLOCK before you upload — problem.md ships as written.
-->

# <Title>

<!-- Describe the hidden system in your own words: what it accepts, what it
     returns, and what is known about its internal form. State every constant
     the solver is allowed to know (a known modulus, a fixed temperature, a
     stated sampling rate).

     Describe it operationally. The solver must not be able to read the intended
     method — or the error it discriminates against — out of your wording. -->


# What you can use

<!-- Name the modes available and state the budget. Do NOT write out call
     signatures; the container appends those. -->

You can probe the system with the modes listed in your tool guide, including a
free `help` mode that reports the available modes and your remaining budget.

Budgeted probes may be used at most **<N>** times in total.

<!-- <N> must equal Oracle.BUDGET, or the model is being lied to about its own
     budget. -->

# Your task

<!-- State exactly what to recover. One paragraph. No method, no hint. -->

# Output format

<!-- The exact submission shape, matching golden/expected.json element for
     element and in the same order — order is part of the answer. State the
     tolerance and the units. -->

Submit your answer via `submit_answer(answer)`.
